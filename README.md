# Book-Management
A Spring Boot–based Book Management System implementing full CRUD functionality using RESTful APIs. The application follows a layered architecture with Controller, Service, and Repository layers, uses Spring Data JPA for database interaction, and supports API testing via Postman with JSON-based requests and responses.
